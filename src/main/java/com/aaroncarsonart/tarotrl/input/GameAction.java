package com.aaroncarsonart.tarotrl.input;

/**
 * General-use game actions that are used in most any GameMode.
 * create more enums if you need more complex behavior for a given
 * GameMode.
 */
public enum GameAction {
    MOVE_UP,
    MOVE_DOWN,
    MOVE_LEFT,
    MOVE_RIGHT,
    OK,
    CANCEL,
    QUIT,
    NONE
}
